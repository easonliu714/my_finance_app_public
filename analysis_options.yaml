include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - poc/sqlcipher_android/**
    - packages/flutter_pdf_text/**
  errors:
    use_build_context_synchronously: ignore

linter:
  rules:
    prefer_single_quotes: true
    sort_child_properties_last: false
