include: ../../../analysis_options.yaml

# AccountRepository contains SQLite DDL defaults with nested SQL quotes. Keep
# the exception local to the account feature rather than weakening root rules.
linter:
  rules:
    prefer_single_quotes: false
